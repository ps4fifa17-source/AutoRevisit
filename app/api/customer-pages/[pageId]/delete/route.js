import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
export async function POST(request,{params}){ const supabase=createClient(); const {data:auth}=await supabase.auth.getUser(); if(!auth.user)return NextResponse.redirect(new URL("/login",request.url)); const {data:profile}=await supabase.from("profiles").select("dealership_id").eq("id",auth.user.id).single(); if(!profile?.dealership_id)return NextResponse.redirect(new URL("/dashboard/live-pages",request.url)); await supabase.from("customer_pages").update({deleted_at:new Date().toISOString(),status:"deleted"}).eq("id",params.pageId).eq("dealership_id",profile.dealership_id); return NextResponse.redirect(new URL("/dashboard/live-pages",request.url)); }
